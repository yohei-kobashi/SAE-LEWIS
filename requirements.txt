# Project-specific dependencies only.
#
# The GH200 ARM environment provides the GPU/ML stack pre-installed and
# build-matched (torch, transformers, accelerate, huggingface_hub,
# safetensors, numpy, tqdm). DO NOT add those here — re-installing via pip
# would pull non-ARM wheels and break CUDA bindings.
#
# Add only libraries that are project-specific and not GPU-related.

wandb>=0.16
sentencepiece>=0.1.99
scikit-learn>=1.3
datasets>=2.16
pysbd>=0.3.4
